[package]
name = "kura-fsync-commit-spike"
version = "0.0.0"
edition = "2021"
publish = false
description = "Throwaway fsync + superblock commit pointer. Not the kura store."

[[bin]]
name = "kura-fsync-commit-spike"
path = "src/main.rs"

[dependencies]
crc32fast = "1"
sha2 = "0.10"
